{
  "name": "cogniview",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "cd confidence-boost-ai && npm run dev",
    "build": "cd confidence-boost-ai && npm run build",
    "preview": "cd confidence-boost-ai && npm run preview"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  }
} 